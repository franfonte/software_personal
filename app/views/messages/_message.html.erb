<div class="cont-<%=message.user.id%>">
  <div class="message-box msg-<%= message.user.id %>">
    <% unless message.room.is_private %>
      <h5 class="name"> <%= message.user.email %> </h5>
    <% end %>
    <%= message.body %>
  </div>
</div>
