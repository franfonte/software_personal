<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Administrador: Solicitudes</title>
  <link rel="stylesheet" href="admin_solicitudes.css">
</head>
<body>
  <header>
    <div class="administrador-solicitudes-heading">ADMINISTRAR SOLICITUDES</div>
  </header>
  <div class="administrador-solicitudes-container">
    <div class="administrador-solicitudes-header">
      <div class="administrador-solicitudes-detail">USUARIO</div>
      <div class="administrador-solicitudes-detail">ACTIVIDAD</div>
      <div class="administrador-solicitudes-detail">FECHA</div>
      <div class="administrador-solicitudes-detail">DESCRIPCIÓN</div>
      <div class="administrador-solicitudes-detail">ESTADO SOLICITUD</div>
      <div class="administrador-solicitudes-detail"></div>
    </div>
    <% Solicitud.all.each do |solicitud| %>
        <div class="administrador-solicitudes-item">
          <div class="administrador-solicitudes-detail"><%= User.find(solicitud.user_id).email %></div>
          <div class="administrador-solicitudes-detail"><%= Actividad.find(solicitud.actividad_id).titulo %></div>
          <div class="administrador-solicitudes-detail"><%= solicitud.created_at.strftime("%d/%m/%y") %></div>
          <div class="administrador-solicitudes-detail"><%= solicitud.descripcion %></div>
          <div class="administrador-solicitudes-detail">
            <% if solicitud.estado %>
              <%= "Aceptada" %>
            <% else %>
              <%= "Pendiente" %>
            <% end %>
          </div>
          <div class="administrador-solicitudes-detail">
            <%= button_to "Eliminar Solicitud", solicitud_path(solicitud.id), method: :delete, class: "administrador-solicitudes-button" %>
          </div>
        </div>
    <% end %>
  </div>
</body>
</html>

